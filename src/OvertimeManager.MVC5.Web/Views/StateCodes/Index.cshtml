@model IEnumerable<OvertimeManager.MVC5.Web.DbModels.StateCodes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StateCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StateCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StateCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.StateCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StateCode })
        </td>
    </tr>
}

</table>
